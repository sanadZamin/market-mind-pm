auto-install-peers=false
strict-peer-dependencies=false

# Include native binaries for all common platforms in the lockfile
# so developers on macOS (ARM/Intel) and Windows can install without errors
supportedArchitectures[]=linux-x64
supportedArchitectures[]=darwin-arm64
supportedArchitectures[]=darwin-x64
supportedArchitectures[]=win32-x64
