# Copy to DEPLOY_DIR/.env on the deploy host (Jenkins never overwrites this file).
# No "export" prefix — plain KEY=value lines only.

PGHOST=149.102.140.178
PGPORT=5432
PGDATABASE=marketmind
PGUSER=postgres
PGPASSWORD=change-me

# Or use Docker DNS when hayyah-postgres is on a shared network (see docker-compose.override.example.yml):
# PGHOST=hayyah-postgres

SMTP_HOST=
SMTP_PORT=465
SMTP_USER=resend
SMTP_PASS=
RESEND_API_KEY=
SMTP_SECURE=true
EMAIL_ENABLED=false
EMAIL_FROM=
PM_TOOL_BASE_URL=https://market-mind.com/pm
